import React from 'react';
import { Box, Typography } from '@mui/material';

const Dashboard: React.FC = () => {
  return (
    <Box>
      <Typography variant='h3'>Dashboard</Typography>
      <p>Conteúdo do painel de administração.</p>
    </Box>
  );
};

export default Dashboard;
